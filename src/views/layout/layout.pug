- const projectConfig = require('/project.config.js')

- const skills = require('/src/views/data/skills.json')
- const spoilers = require('/src/views/data/spoilers.json')
- const projects = require('/src/views/data/projects.json')
- const reviews = require('/src/views/data/reviews.json')
- const socials = require('/src/views/data/socials.json')

- const {
-		default: {
-			entry,
- 		lang: localization
- 	}
-	} = projectConfig
- const production = self.htmlWebpackPlugin.options.production

block variables
	- let title
	- let header
	- let footer
	- let popups
	- let preloader

doctype html
html(lang=localization)
	head
		title=title
		meta(charset='UTF-8')
		meta(
			name='viewport'
			content='width=device-width, initial-scale=1'
		)
		meta(
			name='format-detection'
			content='telephone=no'
		)
		link#favicon(
			rel='icon'
			href='favicon.ico'
		)

		if production
			link(
				rel='stylesheet'
				href='assets/css/style.min.css'
			)

		script(defer src=`assets/js/${entry}.min.js`)
	body
		.wrapper.flex-column
			if header
				include header

			main.page
				block content

			if footer
				include footer

		if preloader
			include ../components/preloader
		if popups
			include ../components/popup
